<nav class="navbar navbar-dark bg-dark">
    <div class="container-fluid">
    <div class="navbar-header">
        <a class="navbar-brand" href="/">99 Gag</a>

        <div class="navbar-text">
                <!-- read it from helper method instead -->
                <!-- <%= session[:current_user_email] || 'No email saved' %> -->
                <%= current_user_email %>
        </div>
    </div>

    <form>
        <button type="button" class="btn btn-success"
                data-toggle="modal" data-target="#exampleModal">Add a post</button>
    </form>
    </div>
</nav>